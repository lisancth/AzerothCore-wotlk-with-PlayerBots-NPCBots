# NpcBot 稳定性增强补丁 2.0 总结

本次更新主要解决了多个地牢副本中出现的“1 滴血不死怪”Bug，并优化了编译器的兼容性，确保补丁能顺利部署。

## 1. C++ 核心逻辑 (bot_ai.cpp)
- **多地图强杀逻辑 (Nuclear Option)**:
  - **支持地图**: 达克萨隆要塞 (Map 271)、影牙城堡 (Map 33)。
  - **支持目标**: 达卡莱复活系列 (26630, 26635, 26620) 及 亡灵哨兵文森特 (4444)。
  - **工作原理**: 实时监测机器人战斗目标，一旦发现目标处于 1-HP 锁血状态或伪死状态（Dead 但仍在战斗），将强制执行 `setDeathState(DeathState(1))`、移除所有光环并调用底层 `Kill` 函数进行物理抹除。
- **编译器兼容性优化**:
  - 使用 `static_cast<uint32>(target->getDeathState())` 解决了强类型枚举比较报错。
  - 使用 `DeathState(1)` 绕过了作用域未定义问题，确保在不同版本的 AzerothCore 源码中均能成功编译。

## 2. 数据库修复 (SQL)
- **达克萨隆要塞**: 彻底清空了问题怪物的 `AIName` 和 `ScriptName`，物理切断导致锁血的 SmartAI 脚本。
- **影牙城堡**: 为文森特（4444）添加了“不可攻击”和“不可选中”标志，彻底防止因 AOE 技能误伤导致的非法复活。
- **路径同步**:
  - `data/customfixsql/`: 存放用于备份和手动参考的描述性 SQL。
  - `data/sql/updates/db_world/`: 存放符合官方命名的自动更新 SQL。

## 3. 部署建议
- **清理锁定**: 由于 `game.lib` 容易被残留进程锁定，请务必在生成前**重启电脑**。
- **重新编译**: 重启后直接在 VS 中进行“重新生成 (Rebuild)”。
- **数据库加载**: 确保服务端启动时自动加载了 `2026_05_16_00` 和 `2026_05_16_01` 补丁。

---
**版本**: v2.0-stability-ultra
**日期**: 2026-05-16
